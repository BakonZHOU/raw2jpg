# Qt 版变更备忘

每次继续修改前先读一遍。

1. 只保留 Qt 版，不恢复 Web 端页面行为。
2. 不要终止服务器按钮；关闭窗口就直接退出程序。
3. 路径选择区不要备注说明，不要显示 `\n` 这类换行标识。
4. `[未选择]` 必须放在按钮右侧横向显示。
5. 拖入文件夹要直接导入路径，不要再弹出选择框。
6. 预览区支持按拖入顺序批量导入：多个文件夹按顺序依次填入 JPG / RAW / 导出目录。
7. 如果某个改动会影响当前逻辑，先停下来确认，不要擅自取巧。
